# Sesión #3 — Fase 2: Eliminar dead code

**Fecha:** 2026-08-01
**Branch:** `refactor/architectural-cleanup`
**Estado:** ✅ Completada

---

## 🎯 Objetivo

Ejecutar la Fase 2 del roadmap: eliminar código muerto identificado en la auditoría:
- `ReporteBtn` (componente definido pero nunca usado).
- `src/AGENTS.md` (asumido duplicado).
- Wrappers `CliEntesTab` y `HistorialTab` (solo agregan padding+header).

## ⚠️ Hallazgo crítico al inicio

`src/AGENTS.md` (2.598 líneas, 149 KB) **NO era documentación**: era código JavaScript/JSX renombrado como backup durante un arreglo fallido (commit `195b921`).

**Evidencia:**
- Empieza con `import { useState, useEffect, useCallback, useRef } from "react";`
- Contiene `const API = "https://script.google.com/..."`
- Termina con `</>);` (cierre de JSX)
- `git log -- src/AGENTS.md` muestra el commit `195b921 arreglo a estado anterior por error en modulo tras arreglo de codigo fallido`.

**Decisión:** consultar al usuario, quien aprobó la opción "Renombrar a `.archive/App.jsx.snapshot-2026-08.md`" — preservar historial, sacar del árbol de código fuente.

## 📦 Cambios realizados (3 commits)

### Commit 2a — `e65b8f3` — Eliminar ReporteBtn

**Cambios:**
- Banner `// ═══ REPORTE BTN ═══` eliminado (líneas 124–126).
- Función `ReporteBtn` completa (líneas 280–330, 51 líneas) eliminada con `sed`.

**Validación:**
- build OK
- 6 tests pasan
- Lint: 24 → 22 errores

**Riesgo:** NINGUNO (código muerto, nadie lo importaba).

### Commit 2b — `a9ebfff` — Eliminar wrappers

**Cambios:**
- En el render principal (líneas 2330–2331), call-sites `<CliEntesTab>` y `<HistorialTab>` reemplazados por `<div>` con header inline + componente directo.
- Definiciones de `CliEntesTab` y `HistorialTab` (líneas 1871–1890, 20 líneas) eliminadas.

**Validación:**
- build OK
- 6 tests pasan
- Bundle: 300.22 kB → 299.96 kB

**Riesgo:** BAJO. Los wrappers solo agregaban padding+header grande, sin lógica. Header movido inline preserva el visual exacto.

### Commit 2c — `1632e48` — Renombrar snapshot histórico

**Cambios:**
- `git mv src/AGENTS.md .archive/App.jsx.snapshot-2026-08.md`
- Directorio `.archive/` creado.

**Validación:**
- build OK (no afecta código)
- git detectó el rename 100%

**Riesgo:** NINGUNO (es solo mover un archivo, no compila).

## 📊 Métricas

| Métrica | Antes Fase 2 | Después Fase 2 | Δ |
|---|---|---|---|
| App.jsx | 2.471 | 2.396 | **-75** |
| Bundle | 300.22 kB | 299.96 kB | -260 B |
| Errores lint | 24 | 22 | -2 |
| Commits rama | 7 | 10 | +3 |

## 📁 Archivos tocados

| Archivo | Cambio |
|---|---|
| `src/App.jsx` | -75 líneas (ReporteBtn + 2 wrappers) |
| `src/AGENTS.md` | renombrado a `.archive/App.jsx.snapshot-2026-08.md` |
| `.archive/App.jsx.snapshot-2026-08.md` | nuevo (era `src/AGENTS.md`) |
| `docs/REFACTOR_ROADMAP.md` | Fase 2 marcada ✅ |
| `docs/REFACTOR_CHANGELOG.md` | nueva entrada Sesión #3 |
| `docs/sessions/2026-08-01-sesion-03-fase-2.md` | este archivo |

## ⚠️ Pendientes (no introducidos por esta fase)

- `src/components/tareas.jsx`: 1 línea modificada por el usuario, sin commitear. No tocado.
- 22 errores de lint pre-existentes restantes: variables no usadas, `setState` en effect, `Buffer is not defined`, etc. Pendientes para Fase 22.

## 🔍 Comandos ejecutados

```bash
# Investigación
ls -la src/AGENTS.md
head -10 src/AGENTS.md
git ls-files src/AGENTS.md
git log --oneline --all -- src/AGENTS.md
diff -q src/AGENTS.md AGENTS.md
wc -l src/AGENTS.md AGENTS.md

# Confirmación con grep
grep -rn "ReporteBtn" src/
grep -n "CliEntesTab\|HistorialTab" src/App.jsx
grep -n "^function Mas\b" src/App.jsx

# Commit 2a
sed -i.bak '280,330d' src/App.jsx  # eliminar ReporteBtn
rm src/App.jsx.bak
npm run build && npm test
git add src/App.jsx
git commit -m "chore(fase-2a): ..."

# Commit 2b
# Edit call-sites (líneas 2330-2331)
# Edit definiciones (líneas 1869-1890)
npm run build && npm test
git add src/App.jsx
git commit -m "chore(fase-2b): ..."

# Commit 2c
mkdir -p .archive
git mv src/AGENTS.md .archive/App.jsx.snapshot-2026-08.md
git commit -m "chore(fase-2c): ..."
```

## ✅ Validación completa

- [x] Branch activo: `refactor/architectural-cleanup`
- [x] Tag `v1.0-pre-refactor` intacto
- [x] 3 commits limpios (`e65b8f3`, `a9ebfff`, `1632e48`)
- [x] `App.jsx` reducido en 75 líneas (2.471 → 2.396)
- [x] Bundle bajó 260 B
- [x] Lint mejoró: 24 → 22 errores
- [x] Build OK, 6 tests OK
- [x] Snapshot histórico preservado en `.archive/`
- [x] Working tree limpio (solo `tareas.jsx` del usuario)
- [x] Roadmap, changelog, bitácora actualizados

## 🎬 Próximos pasos

**Esperando autorización** para **Fase 3: Extraer átomos UI**.

Targets: `Card`, `Btn`, `ChipGroup`, `FInput`, `ConfirmDelete`, `Pill`, `Divider`, `AutocompleteInput` → mover a `src/shared/ui/`.

Riesgo: BAJO (mover componentes, no cambiar lógica).
Impacto: ~150 líneas aisladas en `shared/ui/`.

---

*Fin de la sesión de Fase 2. Tres commits limpios. App.jsx -75 líneas. Snapshot histórico preservado.*