# Sesión #13 — Fase 12: Extraer feature Clientes

**Fecha:** 2026-08-22
**Estado:** ✅ Completada
**Branch:** `refactor/architectural-cleanup`
**Commit:** `refactor(fase-12): extraer feature clients`

## Resumen

Extracción completa de la feature Clientes desde `App.jsx` hacia `src/features/clients/`. Es la feature más grande del refactor hasta ahora: lista, detalle, deuda, abonos, swipe y reporte WhatsApp.

## Archivos nuevos

- `src/features/clients/Clientes.jsx` — orquestador principal.
- `src/features/clients/ClienteDetail.jsx` — detalle del cliente seleccionado.
- `src/features/clients/ClientesListItem.jsx` — fila de cliente.
- `src/features/clients/ClienteStats.jsx` — resumen del período.
- `src/features/clients/ClienteHistorial.jsx` — historial paginado.
- `src/features/clients/DebenCobrarPanel.jsx` — panel superior de deudores.
- `src/features/clients/SwipeableVenta.jsx` — swipe bidireccional.
- `src/features/clients/MarcarPagadoBtn.jsx` — confirmación de pago.
- `src/features/clients/AbonoModal.jsx` — modal de abono.
- `src/features/clients/DeudaFactura.jsx` — factura visual de deuda.
- `src/features/clients/ReporteClienteBtn.jsx` — reporte para WhatsApp.
- `src/features/clients/hooks/useClientesFilter.js` — filtro/paginación con `useMemo`.
- `src/features/clients/hooks/useDeudaPorCliente.js` — agrupación de deuda real.
- `src/features/clients/index.js` — barrel.

## Archivos modificados

- `src/App.jsx` — 1.433 → 949 líneas.
- `docs/REFACTOR_ROADMAP.md`
- `docs/REFACTOR_CHANGELOG.md`
- `docs/sessions/2026-08-22-sesion-13-fase-12.md`

## Decisiones

1. `cuentaParaListaClientes` se mantiene como regla exclusiva de la pantalla Clientes.
2. `useDeudaPorCliente` conserva `deudaTotal` como fuente de deuda real y usa `saldo` solo como fallback.
3. `SwipeableVenta` conserva la regla crítica: izquierda `"NO"`, derecha `"SI"`, tap abre editor.
4. `registrarAbono` no se movió desde `App.jsx` porque es escritura crítica en CLIENTES col F vía `updateCell`.
5. `marcarPagado` no se movió desde `App.jsx` porque depende de persistir INGRESOS por `_row`.
6. No se tocaron `src/components/tareas.jsx`, SQL ni Supabase.

## Validación

- `npm run build` → OK, bundle 301.01 kB.
- `npm test` → 6/6 tests pasan.
- `npm run lint` → falla por 25 errores + 1 warning existentes/no relacionados. `src/features/clients/` no aparece con errores.

## Próximos pasos

1. Esperar autorización para **Fase 13: Extraer feature History**.
2. Mantener SQL/Supabase para una fase posterior al cierre de la refactorización.
