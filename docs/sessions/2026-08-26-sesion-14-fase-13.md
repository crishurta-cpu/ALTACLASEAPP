# Sesión #14 — Fase 13: Extraer feature History

**Fecha:** 2026-08-26
**Estado:** ✅ Completada
**Branch:** `refactor/architectural-cleanup`
**Commit:** `4d479ed` — `refactor(fase-13): extraer feature history`

---

## Contexto

Codex había adelantado la Fase 13 (creó los 5 archivos en `src/features/history/`) pero:
1. NO integró el componente en `App.jsx`.
2. NO hizo commit.
3. Modificó sin querer el color del botón en `src/components/tareas.jsx` (de `#2563eb` a `rgb(156 125 57)`).

Esta sesión terminó la fase: integró `Historial` en App.jsx, revirtió el cambio accidental en `tareas.jsx`, validó build/tests, y commiteó.

---

## Archivos modificados

- `src/App.jsx` (**-112 líneas**: 949 → 837)
- 5 archivos nuevos en `src/features/history/`:
  - `Historial.jsx` (45 líneas con JSDoc) — orquestador
  - `MesAccordion.jsx` (91 líneas con JSDoc) — acordeón mensual
  - `FiltrosHistorial.jsx` (56 líneas con JSDoc) — controles
  - `hooks/useHistorialFilter.js` (62 líneas con JSDoc) — hook con doble `useMemo`
  - `index.js` (5 líneas) — barrel

## Cambios realizados

**1. Revertir color en `tareas.jsx`** (1 línea): preserva la "modificación del usuario" registrada en Fase 0.

**2. Agregar import en App.jsx:**
```js
import Historial from "./features/history/Historial";
```

**3. Reemplazar función inline `Historial` (114 líneas)** por comentario señalador:
```js
// Historial: ver ./features/history/Historial.jsx
```
Mismo patrón que Clientes (Fase 12) y BusquedaGlobal (Fase 11).

**4. `Historial`**: 5 estados locales (open, filter, buscar, categFiltro, orden). Antes vivían inline en App.jsx.

**5. `MesAccordion`**: recibe estados por props (no los replica). `resetAndToggle` centraliza reset al abrir/cerrar.

**6. `FiltrosHistorial`**: recibe `gastos`, `catEntries`, `categDisponibles` ya calculados. Solo UI.

**7. `useHistorialFilter`**: dos `useMemo` separados:
- `months` depende solo de db.
- El resto depende de mes + filtros + búsqueda + orden.

## Decisiones tomadas

- **`useHistorialFilter` con doble `useMemo`**: optimiza re-renders. `months` no se recalcula en cada keystroke.
- **`GraficoCircular` reusado** desde `shared/charts/` (Fase 4).
- **Imports con paths explícitos** desde App.jsx — patrón consistente con Fases 5–12.
- **NO se movió lógica a constantes**: `PIE_COLORS` se quedó en `FiltrosHistorial.jsx` (es estática local del componente).

## Problemas encontrados

- **Codex dejó la fase a medias**: archivos creados pero no integrados, sin commit, y modificó color del botón en `tareas.jsx`. Detectado al revisar `git status` y el diff vs HEAD. Resuelto en esta sesión.
- **`tareas.jsx`**: la línea modificada (`rgb(156 125 57)` en lugar de `#2563eb`) revertida. El roadmap en Fase 0 explícitamente instruye: "NO interferir con esa modificación del usuario".

## Validación

- [x] `npm run build` OK (302.05 kB)
- [x] `npm test` 6 tests pasan
- [x] Comportamiento idéntico preservado (tabs ingresos/gastos, búsqueda, filtro por categoría, orden por fecha/monto, gráfico circular, reset al toggle)

## Próximos pasos

1. **Esperar autorización** para iniciar **Fase 14: Extraer feature Home**.
2. Targets: Home.jsx (orquestador) + 8 sub-componentes + `hooks/useHomeStats`.
3. Riesgo: MEDIO (lógica grande pero menos subdivisión que Clientes).
4. Mantener fuera: SQL (`BASE DE DATOS COPIA.sql`), `supabase/` y `docs/ALTACLASE_DATABASE_ARCHITECTURE.md` — son trabajo de Fase 15+ / fuera del refactor actual.
