# Sesión #11 — Fase 10: Extraer feature Personal

**Fecha:** 2026-08-22
**Estado:** ✅ Completada
**Branch:** `refactor/architectural-cleanup`
**Commit:** `refactor(fase-10): extraer feature personal/`

## Resumen

Extracción de `Personal` (vista Deuda Valen) y `DeudaPersonalForm` (modal compartido) desde `App.jsx` a `src/features/personal/`. Esta feature está **separada del negocio a propósito** — no afecta las métricas de Home/Historial/Clientes.

## Archivos nuevos

- `src/features/personal/Personal.jsx` (108 líneas) — vista con saldo actual + lista
- `src/features/personal/DeudaPersonalForm.jsx` (133 líneas) — modal agregar/editar
- `src/features/personal/index.js` (6 líneas) — barrel con comentario

## Archivos modificados

- `src/App.jsx` — **-82 líneas netas** (1.589 → 1.507)
- `docs/REFACTOR_ROADMAP.md` — Fase 10 marcada ✅
- `docs/REFACTOR_CHANGELOG.md` — entrada de Sesión #11
- `docs/sessions/2026-08-22-sesion-11-fase-10.md` — este archivo

## Decisiones

1. **`Personal` encapsula cálculo del saldo actual**: `items.length > 0 ? items[items.length-1].saldo : 0`. Antes vivía inline en App.jsx.

2. **`DeudaPersonalForm` encapsula lógica de recálculo**: el `base` se calcula distinto en modo edición vs creación. Esto estaba disperso entre App.jsx y el modal; ahora vive en el modal.

3. **Card con color custom `#1d0909`** (rojo oscuro): distinción visual del negocio. Decisión preservada.

4. **NO usa parsers ni servicios**: Deuda Valen es local-only en esta versión. La migración futura a Supabase agregará servicios.

5. **Barrel con comentario**: documenta que está separada del negocio a propósito.

## Validación

- `npm run build` → ✓ 299.98 kB (sin crecimiento)
- `npm test` → 6/6 tests pasan
- `npm run lint` → 0 errores nuevos en `features/personal/`

## Bundle size

- Antes: 299.98 kB
- Después: 299.98 kB
- Δ: 0 kB (movimiento puro)

## Próximos pasos

1. Esperar autorización para **Fase 11: Extraer feature Search**.
2. Targets: `BusquedaGlobal` → `src/features/search/`.
3. Riesgo esperado: BAJO (componente aislado).
