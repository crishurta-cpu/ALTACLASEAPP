import { createContext } from "react";

// Separados a propósito: ver comentario en ToastProvider.jsx.
export const ToastStateContext = createContext(null);
export const ToastDispatchContext = createContext(null);
