# Sesión #5 — Fase 4: Extraer gráficos

**Fecha:** 2026-08-01 (continuación)
**Branch:** `refactor/architectural-cleanup`
**Estado:** ✅ Completada

---

## 🎯 Objetivo

Mover `GraficoPuntos` y `GraficoCircular` de `App.jsx` a `src/shared/charts/`, uno por archivo, con JSDoc y un barrel.

## 📦 Cambios realizados (3 commits)

### Commit 4.1 — `3dc1e43` — GraficoPuntos

- **Archivo nuevo**: `src/shared/charts/GraficoPuntos.jsx` (160 líneas con JSDoc)
- **App.jsx**: 156 líneas eliminadas (función completa + 2 banners)
- SVG line chart con escala automática desde 0, área rellena, puntos con etiquetas de valor y fecha

### Commit 4.2 — `f69fc94` — GraficoCircular

- **Archivo nuevo**: `src/shared/charts/GraficoCircular.jsx` (118 líneas con JSDoc)
- **App.jsx**: 36 líneas eliminadas (función completa + 2 banners)
- SVG pie chart con leyenda de categorías y porcentajes
- **Issue detectado durante extracción**: `sed` borró 1 línea de más (un `}` huérfano del banner HISTORIAL anterior). Detectado por build fallido, corregido manualmente con `Edit`. Build verde.

### Commit 4.3 — `c3a6d40` — Barrel index.js

- **Archivo nuevo**: `src/shared/charts/index.js` (5 líneas)
- App.jsx sin cambios (decisión: imports individuales por ahora)

## 📊 Métricas

| Métrica | Antes Fase 4 | Después Fase 4 | Δ |
|---|---|---|---|
| App.jsx | 2.287 | 2.097 | **-190** |
| Bundle | 299.97 kB | 299.96 kB | -10 B |
| Lint | 22 | 20 | -2 (variables locales eliminadas) |
| Archivos `shared/charts/` | 0 | 3 | +3 |
| Commits rama | 19 | 22 | +3 |

## 📁 Estructura creada

```
src/shared/
├── ui/         (Fase 3)
└── charts/     ← NUEVO (Fase 4)
    ├── GraficoPuntos.jsx
    ├── GraficoCircular.jsx
    └── index.js
```

## ⚠️ Issues detectados

1. **Build roto durante extracción de GraficoCircular**: `sed '645,680d'` borró 1 línea de más (probablemente el `}` que cerraba el bloque anterior). Detectado por `npm run build`. Resuelto con `Edit` manual eliminando la `}` huérfana.
2. **`K.grafico` redundante con `K.muted`**: ambos tienen el valor `#6b7280`. Detectado durante la extracción. No bloqueante, registrado para Fase 22 (Limpieza final).

## ✅ Validación completa

- [x] 3 commits limpios (4.1, 4.2, 4.3)
- [x] Build OK en cada commit (tras fix manual en 4.2)
- [x] Tests 6/6 OK
- [x] Lint: 22 → 20
- [x] Visual idéntico (sin cambios funcionales, solo movimiento)
- [x] Working tree limpio

## 🎬 Próximos pasos

**Esperando autorización** para **Fase 5: Extraer feature Settings**.

Targets: `Configuracion`, `AccentPicker` → `src/features/settings/`.

Riesgo: BAJO (componente aislado).
Impacto: ~100 líneas aisladas, primera feature en carpeta propia.

---

*Fin de la sesión de Fase 4. 2 gráficos extraídos. App.jsx -190 líneas más. Total refactor: -590 líneas (de 2.687 a 2.097).*